# Removed Nacos config. Keep minimal bootstrap file if needed; otherwise you can delete this file.
spring:
  application:
    name: anime
debug: false
# If you had Nacos bootstrap settings previously, remove them.