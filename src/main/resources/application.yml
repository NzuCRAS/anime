server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 数据库连接信息从 Nacos 获取
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  redis:
  # Redis 配置从 Nacos 获取