```angular2html
┌─────────────────────────────── 前端（浏览器） ───────────────────────────────┐
│                                                                           │
│  ACGN 网站页面（已有）                                                    │
│   - 首页 / 番剧详情 / 收藏 / 日记                                         │
│                                                                           │
│  新增：实时通信页面                                                       │
│   - 好友/群列表页 / 聊天会话页（WebSocket）                               │
│                                                                           │
│  新增：视频模块页面                                                       │
│   - 视频广场 / 上传页 / 普通播放页 / ABR 实验页                           │
│   （HLS/DASH 播放器 + telemetry 上报 + 调用 /recommendation）             │
│                                                                           │
└─────────────────────↑ HTTP/WS ↑────────────────────────────────────────────┘
                      │
                      │
┌───────────────────────────── 后端（Java / Spring Boot） ───────────────────┐
│                                                                           │
│  API 层（Controller）                                                     │
│   - /api/chat/...            ⇒ 实时通信（REST + WebSocket 握手）         │
│   - /ws/chat                 ⇒ WebSocket 端点                             │
│   - /api/videos/...          ⇒ 视频元数据/上传/点赞/播放                  │
│   - /api/abr/...             ⇒ Telemetry 上报 / 推荐码率 / 实验控制       │
│                                                                           │
│  业务层（Service）                                                       │
│   - ChatService：好友/群/消息持久化、历史记录                            │
│   - WebSocketChatHandler：消息分发、在线状态                             │
│   - VideoService：视频上传、MinIO 路径管理、转码任务调度                 │
│   - AbrService：ABR 推荐策略、Telemetry 聚合、实验数据统计               │
│                                                                           │
│  基础组件                                                                │
│   - MinIO Client：读写视频原始文件 & 分段文件                             │
│   - Redis：缓存（会话信息、在线用户、点赞计数、转码任务队列）            │
│   - MySQL：用户、好友、群、消息、视频元数据、Telemetry、实验结果          │
│                                                                           │
└───────────────────────────────────────────────────────────────────────────┘

             │                 │                        │
             │                 │                        │
     ┌──────────────┐  ┌───────────────┐        ┌──────────────┐
     │   MinIO      │  │    Redis      │        │    MySQL      │
     │ (对象存储)   │  │(缓存/队列)     │        │(主业务数据)   │
     └──────────────┘  └───────────────┘        └──────────────┘
```